# Define two lists
list1 = [1, 2, 3, 4, 5]
list2 = [4, 5, 6, 7, 8]

# Use a list comprehension to find the intersection
# The intersection contains elements that are in both lists
intersection = [item for item in list1 if item in list2]

# Print the result
print("The intersection of the two lists is:", intersection)
